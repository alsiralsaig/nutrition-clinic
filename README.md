# 🥗 نظام إدارة عيادة التغذية — Nutrition Clinic

تطبيق ويب كامل (خادم + قاعدة بيانات + واجهة عربية RTL) لتنظيم عمل عيادة تغذية:
ملفات المرضى، الزيارات والقياسات، البرامج الغذائية، المواعيد، المدفوعات، التقارير، الحماية، والنسخ الاحتياطي —
**ومبني بحيث يُربط لاحقاً بتطبيق Android/iPhone خاص بالمريض على نفس قاعدة البيانات.**

> لا يشبه جدول Excel: شاشات مستقلة، بطاقات مؤشرات، رسوم بيانية، محرر خطط غذائية، وأوراق طباعة A4 جاهزة.

---

## 1) التشغيل السريع

```bash
git clone https://github.com/alsiralsaig/nutrition-clinic.git
cd nutrition-clinic
npm install                 # يثبّت الخادم والواجهة معاً (npm workspaces)
npm run dev                 # يشغّل الخادم على 4000 والواجهة على 5173
```

افتح: **http://localhost:5173**

| الحساب | كلمة المرور | الصلاحية |
|---|---|---|
| `admin` | `admin123` | كل شيء: حذف، مستخدمون، إعدادات، استعادة نسخ |
| `doctor` | `doctor123` | أخصائي: إضافة وتعديل كل السجلات الطبية والمالية |
| `reception` | `reception123` | مشاهدة فقط (قراءة وتقارير وطباعة) |

⚠️ **أول خطوة بعد التشغيل:** من `الإعدادات ← المستخدمون والصلاحيات ← تغيير كلمتي السرية` غيّر كلمات المرور التجريبية.

البيانات التجريبية (14 مريضاً بزياراتهم وقياساتهم وخططهم ومدفوعاتهم + حسابا doctor/reception) تُزرع تلقائياً
عند أول تشغيل **محلي** لقاعدة فارغة. على قاعدة الإنتاج (Neon عبر `DATABASE_URL`) لا تُزرع: يُنشأ حساب `admin` وحده
وتظهر لافتة حمراء حتى تُغيَّر كلمة مروره. `SEED_DEMO=1` أو `0` يفرض الخيار في أي بيئة.
لبدء قاعدة محلية نظيفة احذف المجلد `apps/api/.data/pgdata` ثم أعد التشغيل.

### الوضع المُنتِج (منفذ واحد فقط)

```bash
npm run build          # يبني الواجهة في apps/web/dist
npm run start          # Express يقدّم الواجهة + الـ API على http://localhost:4000
```

---

## 2) البنية

```
nutrition-clinic/
├── apps/
│   ├── api/                     # Express + PostgreSQL (pg / PGlite) + JWT
│   │   ├── src/server.js        # نقطة التشغيل وتقديم واجهة الويب بعد البناء
│   │   ├── src/schema.sql       # مخطط PostgreSQL (11 جدولاً + منظر) — يُطبَّق تلقائياً عند الإقلاع
│   │   ├── src/db.js            # طبقة قاعدة واحدة: Neon/Postgres أو PGlite محلياً + معاملات + تدقيق
│   │   ├── src/auth.js          # JWT + bcrypt + أدوار
│   │   ├── src/lib.js           # كل الحسابات (BMI، الماكرو، الإجماليات) والتحقق
│   │   ├── src/seed.js          # حسابات افتراضية + بيانات تجريبية
│   │   ├── src/routes/          # patients · measurements · plans · appointments
│   │   │                        # payments · dashboard · reports · auth · system
│   │   ├── openapi.json         # توثيق الـ API (لتوليد كود تطبيق الموبايل)
│   │   └── test/smoke.test.js   # 85 فحصاً على مستوى الـ API
│   └── web/                     # React + Vite + Tailwind (عربي RTL)
│       ├── src/pages/           # Login · Dashboard · Patients · PatientFile
│       │                        # Appointments · Plans · Payments · Reports · Settings
│       ├── src/components/      # Layout · ui · Charts · Forms · PrintDocs · PatientForm
│       ├── src/api.js           # عميل REST موحّد
│       └── test/ui.test.mjs     # 44 فحصاً يشغّل الواجهة فعلياً في jsdom
├── package.json                 # أوامر الجذر (dev · build · start · seed · test)
└── .github/workflows/ci.yml     # بناء + اختبارات على PostgreSQL 17 حقيقي في GitHub Actions
```

**قاعدة البيانات: PostgreSQL فقط، بمحركين يتكلمان نفس اللهجة** (نفس الكود ونفس الاستعلامات):

| البيئة | المحرك | أين تُحفظ البيانات |
|---|---|---|
| الإنتاج (Vercel + Neon) | `pg` عبر `DATABASE_URL` | Neon — دائمة، ومنها سيقرأ تطبيق الموبايل |
| تشغيل محلي بلا إعداد | PGlite (Postgres مدمج داخل Node، لا يحتاج تثبيتاً) | `apps/api/.data/pgdata` على جهازك |
| Vercel **بدون** قاعدة مربوطة | PGlite في الذاكرة | لا مكان — وضع تجريبي بلافتة صفراء |

أي Postgres ≥ 14 يعمل كما هو (Neon، Supabase، Railway، خادم خاص): فقط ضع رابطه في `DATABASE_URL`.

---

## 3) الوظائف المنفَّذة (مطابقة للمتطلبات)

### لوحة التحكم الرئيسية
* بطاقات: عدد المرضى، النشطون، مواعيد اليوم، إيرادات الشهر، إجمالي الإيرادات.
* إحصائيات المتابعة: من قاسوا هذا الشهر، زيارات 30 يوماً، البرامج النشطة، متوسط نزول الوزن، وتنبيه «مرضى بلا متابعة منذ 60 يوماً».
* رسوم: نمو عدد المرضى (جديد + تراكمي)، تطور متوسط الوزن/BMI/نسبة الدهون، الإيرادات الشهرية، توزيع فئات BMI، ازدحام أيام الأسبوع، جدول مواعيد اليوم بأزرار تنفيذ سريعة، وقائمة أفضل نتائج النزول.

### إدارة المرضى
* رقم ملف تلقائي `NC-0001` (قيد فريد في القاعدة — لا تكرار ولا تعديل يدوي).
* الاسم واللقب، الهاتف، تاريخ الميلاد (مع حساب العمر)، الجنس، الطول، وزن البداية، الوزن المستهدف، الهدف الغذائي، الملاحظات، حالة الملف.
* بحث فوري بالاسم أو رقم الملف أو الهاتف + فرز (الأحدث/الاسم/رقم الملف) + ترقيم صفحات + تصفية بالحالة.
* شريط بحث عام في رأس الصفحة يقترح المرضى أثناء الكتابة.

### ملف المريض في صفحة واحدة
يُحمَّل من طلب واحد `GET /api/patients/{id}/profile` ويتنقّل داخله بشريط أقسام ثابت:
**نظرة عامة** (مؤشرات + شريط «قطعته من الهدف») · **البيانات** · **القياسات** · **البرنامج الغذائي** · **الزيارات السابقة** · **المواعيد** · **المدفوعات** · **الملاحظات** (تحرير مباشر).
لكل قسم زر إضافة/تعديل مناسب، وأي موعد يُعلَّم «تمت» تُنشأ له زيارة تلقائياً.

### المتابعة والقياسات
الوزن، الطول، **BMI تلقائي**، محيط الخصر، الصدر، الورك، **نسبة الدهون**، **نسبة الخصر/الورك**، ملاحظات، وربط كل قياس بالزيارة.
رسم بيانية تفاعلية لتطور الوزن/BMI/الخصر/الدهون مع أزرار إظهار/إخفاء لكل مقياس، وجدول يعرض الفارق عن الزيارة السابقة وعن البداية.

### البرامج الغذائية
محرر بصفوف وجبات (الوجبة، التوقيت، اسم الطبق، الأصناف، الكميات، سعرات، بروتين، كربوهيدرات، دهون):
* المجاميع والإحصاء مقابل الهدف يُحسبان لحظياً، مع تلوين الانحراف عن الهدف بأكثر من 8٪.
* إضافة أصناف من جدول أغذية مبسّط يعيد حساب السعرات والماكرو تلقائياً.
* اعتماد خطة (خطة نشطة واحدة لكل مريض)، أرشفة، **نسخ خطة لمريض آخر**، تعديل، حذف.
* **طباعة / حفظ PDF**: ورقة A4 بترويسة العيادة + جدول البدائل + خانة توقيع وختم.

### المواعيد
جدول يوم مقسَّم إلى أنصاف ساعات مع إبراز الأوقات الشاغرة والحجز منها بنقرة، عرض أسبوعين مع تغيير الحالة من الجدول،
تسجيل/تعديل/حذف، نوع الزيارة، المدة، ملاحظات، **منع تداخل نفس الوقت (409)**، وملخص: مجدول/مؤكد/تمت/ملغي/لم يحضر.

### المدفوعات
المبلغ، نوع الخدمة، تاريخ الدفع، طريقة الدفع (نقدي/بطاقة/تحويل/محفظة هاتف/تقسيط)، رقم الفاتورة، موظف مُدخِل الدفعة.
حساب الإيرادات: إيراد الفترة، المتوسط، إيراد اليوم والشهر، أعلى الخدمات، التقسيم حسب الطريقة — مع **إلغاء بدل حذف** (يبقى الأثر) وحذف نهائي للمدير فقط.

### التقارير
تقرير مريض كامل · تقرير تطور الوزن · تقرير الزيارات · تقرير الإيرادات · سجل التدقيق.
لكل تقرير معاينة على الشاشة ثم **طباعة أو حفظ PDF** بورقة A4 (ترويسة العيادة، تكرار رؤوس الأعمدة، تذييل بالتوقيع).
**تصدير CSV** (بترميز UTF-8 + BOM ليعرب في Excel) للمرضى والقياسات والمدفوعات والمواعيد.

### الحماية
* شاشة دخول باسم مستخدم وكلمة مرور (bcrypt) + JWT صلاحيته 12 ساعة.
* ثلاثة أدوار: `admin` (كل شيء) · `staff` (تعديل بلا إدارة مستخدمين/حذف نهائي/استعادة) · `viewer` (قراءة فقط).
* **حماية الصيغ:** `BMI`، `BMR`/الماكرو، `waist_hip_ratio`، إجماليات البرنامج، والإيرادات — **تُحسب في الخادم فقط**؛ أي قيمة يحقنها العميل تُتجاهَل (مغطّاة باختبار).
* حقول مشتقة للقراءة فقط في الواجهة، لا يمكن لمسها.
* حماية الأرشيف الطبي: غير المدير لا يحذف ملف مريض (أرشفة بدل التدمير).
* **سجل تدقيق** لكل عملية (من، متى، ماذا، على أي سجل).
* **نسخ احتياطي**: تنزيل JSON لكل الجداول، تنزيل ملف القاعدة الخام، `VACUUM + integrity_check`، و**استعادة** مع حفظ نسخة أمان تلقائية قبل التنفيذ.
* حدّ محاولات دخول على مستوى الـ IP، و`express.json` بحد حجم، وكل الاستعلامات prepared statements (لا حقن SQL).

### التصميم
عربي RTL بالكامل، خط Tajawal/Cairo، لوحة ألوان هادئة (أخضر مائي + رملي + لمسات عنبرية)، بطاقات وظلال ناعمة،
أزرار واضحة كبيرة، تنبيهات Toast، وحالات فارغة مرشدة — بلا إحساس جداول Excel.

---

## 4) أوامر مفيدة

| الأمر | الوظيفة |
|---|---|
| `npm run dev` | خادم + واجهة مع Hot Reload |
| `npm run build` | بناء الواجهة في `apps/web/dist` |
| `npm run start` | تشغيل كل شيء من منفذ واحد (4000) |
| `npm run seed` | بذرة/بيانات تجريبية (أضف `-- --force` لإعادة توليد المرضى) |
| `npm run test:api` | 236 فحصاً على مستوى الـ API (smoke 85 + features 85 + portal 66) |
| `npm run test:ui` | 90 فحصاً يشغّل الواجهة في jsdom (يتطلب `npm run start` يعمل ببيانات جديدة) |
| `npm run test:portal-ui` | 54 فحصاً: نافذتا الأخصائي والمريض معاً — QR، العادات، الانتظار، إشارات المكالمة (يتطلب خادماً جديداً مع `WHATSAPP_DRY_RUN=1`) |

### متغيرات البيئة

```bash
PORT=4000                 # منفذ الخادم
HOST=0.0.0.0
DATABASE_URL=postgres://…  # Neon/Postgres (يضيفه Vercel تلقائياً عند ربط Neon). بدونه: PGlite محلي
CLINIC_DATA_DIR=apps/api/.data       # مجلد PGlite المحلي
SEED_DEMO=1|0             # فرض/منع البيانات التجريبية (الافتراضي: نعم محلياً، لا مع DATABASE_URL)
CLINIC_TZ=Africa/Khartoum # توقيت «اليوم» للمواعيد والإيرادات (خوادم Vercel تعمل بـ UTC)
PG_POOL_MAX=3             # حجم مجمع الاتصالات (3 على Vercel افتراضياً)
JWT_SECRET=<سلسلة-طويلة>  # إن لم تُضف يُولَّد سرّ ويُحفظ داخل القاعدة نفسها
JWT_TTL=12h
CORS_ORIGIN=*             # اضبطه على نطاق تطبيق الموبايل/الموقع في الإنتاج
ADMIN_PASSWORD=...        # كلمة مرور admin عند أول بذرة
WEB_DIST=apps/web/dist    # مجلد الواجهة المقدمة من Express

# --- واتساب والتذكيرات (اختيارية؛ بدونها تعمل الأزرار عبر روابط wa.me) ---
WHATSAPP_TOKEN=...            # توكن دائم من Meta for Developers ← WhatsApp ← System User
WHATSAPP_PHONE_ID=...         # Phone number ID لرقم العيادة في WhatsApp Business
WHATSAPP_TEMPLATE_APPT=...    # قالب معتمد لتذكير الموعد: {{1}} الاسم {{2}} التاريخ {{3}} الوقت
WHATSAPP_TEMPLATE_DAILY=...   # قالب معتمد للتذكير اليومي: {{1}} الاسم {{2}} ملخص وجبات اليوم
WHATSAPP_TEMPLATE_LANG=ar     # لغة القوالب
WHATSAPP_DRY_RUN=1            # تجربة دون إرسال فعلي
CRON_SECRET=<سلسلة-طويلة>     # Vercel يرسله تلقائياً لمسار /api/cron/daily

# --- المرحلة D: البوابة والاستشارات المرئية (اختيارية) ---
PUBLIC_URL=https://nutrition-clinic-ecru.vercel.app   # أساس روابط QR وواتساب (بدونه: من عنوان الطلب)
PATIENT_JWT_TTL=30d           # مدة جلسة المريض على هاتفه
TURN_URL=turns:turn.example.com:5349   # خادم TURN (مفصولة بفواصل) — ضروري لبعض شبكات الجوال
TURN_USERNAME=...
TURN_CREDENTIAL=...
```

---

## 5) ربط تطبيق Android/iPhone (المرحلة القادمة)

الـ API مصمَّم ليكون مصدر الحقيقة الوحيد للويب والموبايل معاً:

1. **الدخول:** `POST /api/auth/login` → `{ token, user }`، ثم أرسل `Authorization: Bearer <token>` في كل طلب.
2. **شاشة المريض كلها من طلب واحد:** `GET /api/patients/{id}/profile` يرجّع البيانات، البرنامج الغذائي النشط،
   المواعيد، القياسات، سلسلة تطور الوزن، والمدفوعات — وهو نفس الطلب الذي تستعمله واجهة الإدارة.
3. **العقد موثّق:** `GET /openapi.json` (39 مساراً + مخططات الحقول) → ولّد منه كود الشبكة:
   ```bash
   npx @openapitools/openapi-generator-cli generate -i apps/api/openapi.json -g dart-dio -o ./mobile/api_client
   # أو: -g typescript-axios / -g kotlin / -g swift5
   ```
4. **حساب المريض:** الجدول جاهز — أضف عموداً مثل `patients.access_code` ومسار `POST /api/patient/login`
   يُصدر نفس التوكن مع حصر الوصول في `/{patient_id}/...` الخاص بالمريض، ويبقى كل شيء آخر بلا تعديل.
5. **إشعارات المتابعة:** أضف FCM/APNs، واستمع في التطبيق لتغيّر `diet_plans.status` أو اقتراب `appointments.date`.
6. **HTTPS إلزامي** في الإنتاج (reverse proxy مثل nginx/Caddy) لأن التوكن يرسل في الهيدر.

للقراءة السريعة من جهاز الموبايل التجريبي أثناء التطوير: شغّل `npm run start` وفتّح نفقاً مؤقتاً
(`ngrok http 4000` أو `cloudflared tunnel`) ثم ضع عنوانه في إعداد التطبيق.

---

## 6) قاعدة البيانات PostgreSQL — كيف بُنيت الطبقة

* `db.js` يعرض واجهة واحدة async: `db.all / get / run / insert / exec / tx`. المعاملات تُمرَّر عبر
  `AsyncLocalStorage`، فكل استدعاء `db.*` داخل `db.tx(async () => …)` يذهب تلقائياً لنفس الاتصال.
* الاستعلامات تُكتب بـ `?` أو `@name` وتُحوَّل إلى `$1..` الخاصة بـ Postgres (مع تخطي النصوص المقتبسة).
* المخطط يُطبَّق عند كل إقلاع **تحت قفل استشاري** (`pg_advisory_xact_lock`) فلا تتصادم حاويات Vercel
  التي تُقلع معاً، ويُتخطّى فوراً إذا كان `_schema_version` مطابقاً (ارفع `SCHEMA_VERSION` عند تعديل المخطط).
* رقم الملف `NC-0001` يُحجز داخل معاملة بقفل، فمريضان يُسجَّلان في نفس الثانية لا يأخذان نفس الرقم.
* يعمل خلف **PgBouncer بوضع transaction** (وهو ما يفعله رابط Neon المجمَّع) — مُختبَر فعلياً.
* أخطاء Postgres تُترجم لرسائل عربية: تكرار ← 409، سجل مرتبط غير موجود ← 400، القاعدة غير متاحة ← 503.

**الاختبارات على 3 طبقات** (كلها في CI على PostgreSQL 17 حقيقي):
```bash
npm run test:api                                        # 85 فحصاً — PGlite
DATABASE_URL=postgres://… SEED_DEMO=1 npm run test:api  # نفس الـ 85 على Postgres حقيقي
SIM_DATABASE_URL=postgres://… npm run deploy:sim        # 22 فحصاً: حاويات Vercel + بقاء البيانات
```

---

## 7) النسخ الاحتياطي في التشغيل اليومي

* من `الإعدادات ← النسخ الاحتياطي`: «نسخة JSON» أو «تنزيل ملف النسخة» — كل الجداول في ملف واحد يُستعاد في أي قاعدة Postgres.
* Neon نفسه يحتفظ بسجل يسمح بالرجوع لنقطة زمنية قريبة (Point-in-time restore) من لوحة Neon.
* الاستعادة من الواجهة (مدير فقط)، وقبلها تُحفظ **نسخة أمان داخل القاعدة نفسها** (آخر 5 في `backup_snapshots`،
  تُنزَّل من `GET /api/backup/snapshots/{id}`) — لأن Vercel لا يملك قرصاً يُحفظ عليه ملف.
* نسخة يومية خارجية اختيارية: `pg_dump "$DATABASE_URL" > clinic-$(date +%F).sql` من أي جهاز.

---

## 7ب) ميزات المرحلة C (الإصدار 2.1)

| الميزة | أين | ملاحظات |
|---|---|---|
| مولّد خطة أسبوعية ذكية | ملف المريض ← «توليد خطة أسبوعية» | Mifflin-St Jeor × مستوى النشاط، ماكرو حسب الهدف، حد أدنى آمن للسعرات، 7 أيام × 5 وجبات، تُحفظ مسودة |
| ملاحظات صوتية | ملاحظات المريض/الزيارة/النموذج 🎙️ | Web Speech API (Chrome/Edge/Safari)، عربي أو English |
| واتساب آلي | أزرار «واتساب» + الإعدادات ← واتساب والتذكيرات | WhatsApp Cloud API؛ بدون مفاتيح: روابط wa.me جاهزة |
| تذكير المواعيد + تذكير يومي | Vercel Cron يومياً 05:00 UTC (7:00 الخرطوم) + جرس 🔔 وإشعارات المتصفح | Hobby يسمح بمهمة يومية واحدة؛ التنبيهات الفورية من الجرس |
| قائمة التسوق | ملف المريض ← البرنامج الغذائي | تجميع الكميات × عدد الأيام؛ طباعة/نسخ/واتساب |
| تقييم الالتزام | كل زيارة (0–100%) | رسم الالتزام مقابل التقدّم الأسبوعي + معامل الارتباط r |
| لوحة الإيرادات | التقارير ← تقرير الإيرادات | Recharts: حسب الخدمة والشهر، تفاعلية (تصفية/تكبير) |
| تصدير CSV | التقارير ← تصدير Excel / CSV | UTF-8 BOM، عناوين عربية أو English |
| الوضع الداكن | زر 🌙 في الرأس أو الإعدادات ← المظهر | فاتح/داكن/حسب الجهاز؛ الطباعة دائماً فاتحة |
| English | زر EN في الرأس | إضافة Babel (`apps/web/babel-plugin-arabic-i18n.js`) تلف كل نص عربي بـ `__t`؛ القاموس `src/i18n/en.js`؛ فحص التغطية `node apps/web/i18n-extract.mjs --check` |

**تفعيل واتساب الآلي:** 1) حساب Meta Business + تطبيق في developers.facebook.com بمنتج WhatsApp. 2) أضف رقم العيادة وخذ `Phone number ID`. 3) أنشئ System User وتوكن دائماً بصلاحية `whatsapp_business_messaging`. 4) اعتمد قالبي «تذكير موعد» و«تذكير يومي» (الرسائل التي يبدأها النشاط التجاري خارج نافذة 24 ساعة تحتاج قالباً معتمداً). 5) أضف المتغيرات أعلاه في Vercel ثم Redeploy. 6) الإعدادات ← واتساب ← «رسالة تجريبية».

---

## 7ج) ميزات المرحلة D (الإصدار 2.2) — بوابة المريض

| الميزة | أين | كيف تعمل |
|---|---|---|
| **بوابة المريض** | `#/portal` (نفس الموقع، مصممة للجوال) | اليوم · خطتي · تقدّمي · مواعيدي. توكن مريض منفصل لا يفتح أي بيانات للعيادة (403) |
| **رمز QR شخصي** | ملف المريض ← «بوابة المريض» | «إصدار رمز QR» ← يطبع بطاقة أو يرسل بواتساب. المسح بكاميرا الهاتف يفتح البوابة ويسجّل الدخول تلقائياً. بديل: رقم الملف + رمز `XXXX-XXXX`. إصدار رمز جديد أو «إيقاف الوصول» يُخرج كل الأجهزة فوراً. الرموز تُحفظ مشفّرة (hash) ولا تظهر إلا مرة واحدة |
| **متتبع العادات** | البوابة ← «اليوم» | الماء (+ كوب/زجاجة)، النوم، النشاط ونوعه والخطوات. يُحفظ فوراً ويظهر في ملف المريض (بطاقة «العادات اليومية»: رسوم، نسبة تحقيق الأهداف، سلسلة الأيام). الأهداف تلقائية (ماء 35 مل/كغ) أو مخصصة |
| **قائمة الانتظار الافتراضية** | المواعيد ← أسفل الصفحة / البوابة ← «مواعيدي» | المريض يطلب موعداً أقرب (أيام وأوقات مفضلة). عند إلغاء/حذف/تأجيل موعد يُعرض الوقت الشاغر تلقائياً على أول 3 مطابقين بواتساب وفي البوابة — أول من يؤكد يحجز، والباقون يُبلَّغون. الرفض أو انتهاء المهلة يمرّر العرض للتالي |
| **الاستشارة المرئية** | المواعيد ← زر التفاصيل ← «بدء استشارة مرئية» | كاميرا + ميكروفون + مشاركة الشاشة + تبديل الكاميرا. يظهر للمريض «انضم الآن» في البوابة ويصله رابط بواتساب. الصوت والصورة مباشرة بين الجهازين (WebRTC)؛ الخادم ينقل رسائل الإعداد فقط |

**ملاحظة TURN:** معظم الاتصالات تنجح بخوادم STUN المجانية المضمّنة. بعض شبكات الجوال/الشركات تمنع الاتصال المباشر؛ لضمان 100% أضف خادم TURN (مثلاً Metered أو Twilio أو coturn) في `TURN_URL/TURN_USERNAME/TURN_CREDENTIAL`.

**الإعدادات:** `clinic.portal_enabled` (تشغيل البوابة)، `clinic.waitlist_enabled`، `clinic.waitlist_batch` (عدد من يُعرض عليهم، افتراضي 3)، `clinic.waitlist_offer_minutes` (مهلة العرض، افتراضي 120 دقيقة).

**تطبيق الموبايل:** كل مسارات `/api/portal/*` موثّقة في `openapi.json` (v2.2.0) — تطبيق Android/iPhone يستعملها كما هي.

---

## 7د) المرحلة E (الإصدار 2.3) — أفكار مأخوذة من نسخة Google AI Studio

راجعنا مشروع [NutriCare-Clinic](https://github.com/alsiralsaig/NutriCare-Clinic) الذي بناه Google AI Studio. هو واجهة فقط تحفظ في `localStorage` (تضيع البيانات بمسح المتصفح ولا يمكن ربط تطبيق جوال بها)، فلم نعتمده أساساً، بل نقلنا منه ما ليس عندنا:

| الميزة | أين | ملاحظات |
|---|---|---|
| **التاريخ الطبي المنظّم** | نموذج المريض ← «التاريخ الطبي» | الأمراض المزمنة، الحساسية، الأطعمة الممنوعة، الأدوية، فصيلة الدم (أعمدة جديدة في `patients`، مخطط v5). تظهر شريطاً ملوناً أعلى الملف، وفي تقرير المريض المطبوع |
| **تنبيه الحساسية في البرنامج** | محرر البرنامج الغذائي | إن احتوت وجبة كلمة من الحساسية/الممنوعات يظهر تنبيه أحمر، وتُعلَّم الوجبات الجاهزة المتعارضة بـ ⚠ |
| **قوالب جاهزة** | محرر البرنامج ← «📋 قوالب جاهزة» | 3 برامج كاملة (صيام متقطع، تضخيم عضلي، متوسطي) و17 وجبة جاهزة، مع زر تراجع. البيانات في `apps/web/src/templates.js` |
| **الألياف والماء** | محرر البرنامج، الملف، البوابة، الطباعة | عمود «ألياف» لكل وجبة ومجموع يُحسب في الخادم، وهدف ألياف (14 غ/1000 سعرة، 25–38 غ) وهدف ماء (35 مل/كغ، 1.5–4 لتر) في مولّد الخطة. اعتماد البرنامج يحدّث هدف الماء في متتبع العادات بالبوابة (مخطط v6) |
| **قفل الشاشة برمز PIN** | زر القفل في الرأس، Ctrl+L، الإعدادات ← الأمان | يقفل دون تسجيل خروج؛ قفل تلقائي بعد 5–30 دقيقة خمول؛ 5 محاولات خاطئة = تسجيل خروج. الرمز مُجزّأ ومحلي لكل مستخدم ولكل متصفح |

> تصحيح لبيانات AI Studio: البرامج الكاملة هناك مكتوب عليها 1500/2400/1700 سعرة بينما مجموع وجباتها الفعلي 1124/2065/1055. اعتمدنا المجموع الحقيقي في الاسم والهدف.

## 8) ملاحظات وعناصر جاهزة للتوسعة

* الطباعة تعتمد `window.print()` — «حفظ كـ PDF» من صندوق الطباعة في المتصفح (أفضل تنسيق عربي من أي مكتبة PDF).
* `patients.access_code`، جدول أدوية/تحاليل، ورسوم الاشتراكات — كلها إضافات مخطّطية بسيطة دون تغيير الواجهة الحالية.
* الاختبارات تغطي: التحقق من المدخلات، الأدوار، حماية الحقول المشتقة، تعارض المواعيد، حسابات BMI/الماكرو/الإيرادات،
  النسخ والاستعادة، وتصدير CSV.

---

## 9) النشر على Vercel

الإعداد في جذر المستودع: `vercel.json` + **دالة واحدة `api/index.js`** تصدّر تطبيق Express كما هو
(نفس الملفات التي تعمل محلياً). `vercel.json` يوجّه كل `/api/*` إليها (`/api/(.*)` ← `/api/index?__p=$1`)
والدالة تعيد المسار الأصلي قبل Express. الواجهة تُبنى بـ `npm run build` وتُقدَّم من `apps/web/dist`،
وأي مسار آخر يعود إلى `index.html`.

### ربط قاعدة Neon (خطوة واحدة من لوحة Vercel)
1. مشروعك على Vercel ← تبويب **Storage** ← **Create Database** ← **Neon** ← المنطقة الأقرب (مثلاً Frankfurt) ← **Create**.
2. **Connect Project** ← اختر مشروع `nutrition-clinic` وكل البيئات. Vercel يضيف `DATABASE_URL` تلقائياً.
3. **Deployments** ← آخر نشر ← **Redeploy** (المتغيرات الجديدة لا تصل إلا لنشر جديد).
4. افتح الرابط ← `/api/health` يجب أن يُظهر `"engine":"postgres"` و`"mode":"persistent"`.
5. ادخل بـ `admin / admin123` وغيّر كلمة المرور فوراً (اللافتة الحمراء تختفي بعدها).

الجداول تُنشأ وحدها في أول طلب — لا SQL يدوي. العيادة تبدأ نظيفة (بلا مرضى تجريبيين).

| سلوك | بدون Neon (تجريبي) | مع Neon (إنتاج) |
|---|---|---|
| المحرك | PGlite في ذاكرة الحاوية | PostgreSQL على Neon |
| البيانات بعد إعادة تشغيل الحاوية | **تُمسح** وتُزرع بيانات العرض | **محفوظة** |
| الحسابات | admin + doctor + reception | admin وحده |
| سرّ `JWT` | ثابت معلن | `JWT_SECRET` إن وُجد، وإلا سرّ عشوائي محفوظ في القاعدة |
| لافتة | صفراء «وضع تجريبي» | لا شيء (أو حمراء حتى تغيير كلمة المرور) |

> ⚠️ **دالة واحدة فقط — لماذا؟** خطة Hobby ترفض النشر إذا زادت الدوال عن 12
> (`No more than 12 Serverless Functions…`)، وصيغة `api/[[...path]].js` لا يبنيها Vercel (مقطع واحد فقط ثم 404).
> الحل المعتمد: دالة واحدة + rewrite. كل ذلك مولَّد ومفحوص:
>
> ```bash
> npm run deploy:bridges   # يولّد api/index.js ويحذف أي دوال زائدة
> npm run deploy:check     # يفشل إذا زادت الدوال عن 12، أو غاب الـ rewrite، أو بقيت لهجة SQLite
> npm run deploy:sim       # 23 فحصاً عبر محاكي توجيه Vercel (scripts/vercel-local.mjs يقرأ vercel.json نفسه)
> ```
>
> ولأن دوال Vercel لا تخدم شيئاً خارج `/api`، فملف التوثيق متاح أيضاً على `/api/openapi.json`.

## الرخصة

مشروع داخلي للعيادة. أعد النشر بحرية داخل فريقك.
